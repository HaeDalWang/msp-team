# 개발 전용 자료

이 디렉터리는 MSP 주간회고 서비스의 실행·배포에 직접 사용하지 않는 개발 자료를 보관합니다.

- `BACKLOG.md`: 검토했지만 아직 구현하지 않은 작업
- `prototypes/`: 화면 시안과 프로토타입 실행 보조 파일
- `data-import/`: 초기 데이터 검수에 사용한 원본 파일과 생성 결과

`data-import/`에는 고객사, 담당자, 초과근무 등 내부 정보가 포함될 수 있으므로 Git에 커밋하지 않습니다. 서비스 코드에서 이 디렉터리의 파일을 직접 참조하지 마세요.
